<div class="question-container">
  <h2 class="question-show-title"><%= @question.title %></h2>
  <div class="question-id"><%= @question.id %></div>
  <div class="row">
    <div class="question-user-info col-md-1">
      <div class="question-vote">
        <%= link_to image_tag("upvote.png", class: "center-block"), question_upvote_path(@question), method: :post, id: "upvote" %>
        <div class="text-center"><%= @question.reputation %></div>
        <%= link_to image_tag("downvote.png", class: "center-block"), question_downvote_path(@question), method: :post, id: "downvote" %>
      </div>
      <div class="empty-gap"></div>
      <div class="question-favorite text-center">
        <% if current_user.favorite_questions.include?(@question) %>
          <i class="fa fa-star fa-3x favorite-toggle" aria-hidden="true"></i>
        <% else %>
          <i class="fa fa-star-o fa-3x favorite-toggle" aria-hidden="true"></i>
        <% end %>
        <div class='favorite-count'><%= @question.favorites.count %></div>
      </div>
      <%= get_flash %>
    </div>
    <div class="question-content col-md-10">
      <%= @question.content %>
      <div class="empty-gap"></div>
      <% @question.tags.each do |tag| %>
        <%= link_to "#{tag.name}", tag_path(tag), class: "btn btn-primary btn-xs tag-btn" %>
      <% end %>
      <div class="empty-gap"></div>
      <div class="question-actions-users row">
        <div class="col-md-3">
          <%= link_to "Edit", edit_question_path %>
          <%= link_to "Delete", question_path, method: :delete %>
        </div>
        <div class="col-md-3 col-md-offset-6">
          <div>asked <%= @question.created_at %></div>
          <div><%= @question.user.name %></div>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="comment-list col-md-10 col-md-offset-1">
      <% @question.comments.each do |comment| %>
        <%= comment.content %> - <%= comment.user.name %>
      <% end %>
    </div>
  </div>
</div>
<h3><span id="answer-count"><%= @question.answers.count %></span> Answers</h3>
<% @question.answers.each do |answer| %>
  <% next if answer.content.nil? %>
  <hr>
  <div id="answer">
    <%= render partial: 'answer', locals: {answer: answer} %>
  </div>
<% end %>
<%= render 'answer_form' %>